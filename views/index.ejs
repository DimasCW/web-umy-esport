<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tournaments</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Import Russo One font */
        @import url('https://fonts.googleapis.com/css2?family=Russo+One&display=swap');

        /* Apply Russo One font */
        body, h2, .card-title, .card-text, .btn {
            font-family: 'Russo One', sans-serif;
        }

        html, body {
            margin: 0;
            padding: 0;
            height: 100%;
            overflow-x: hidden;
        }
        .banner {
            position: relative;
            width: 100%;
            height: 100vh;
            background: url('/assets/banner.png') no-repeat ;
            background-size: cover;
        }
        .banner-text {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            color: white;
            text-align: center;
        }
        .banner-text h1 {
            font-size: 4rem;
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.7);
        }
        .banner-text p {
            font-size: 1.5rem;
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.7);
        }
        .navbar-nav .nav-link {
        position: relative;
        display: inline-block;
        transition: color 0.3s ease;
    }

    .navbar-nav .nav-link:hover {
        color: #ff4081; /* Pink color when hovered */
    }

    .navbar-nav .nav-link::after {
        content: "";
        position: absolute;
        width: 0;
        height: 2px;
        background-color: #e443a6; /* Pink underline */
        left: 50%;
        bottom: -5px;
        transition: width 0.3s ease, left 0.3s ease;
    }

    .navbar-nav .nav-link:hover::after {
        width: 100%;
        left: 0;
    }

    .card:hover {
        transform: translateY(-10px);
        box-shadow: 0 10px 15px rgba(0, 0, 0, 0.1);
    }

    .glass-card {
        background-color: rgba(255, 255, 255, 0.123); /* Increased transparency for glass effect */
        backdrop-filter: blur(15px); /* Stronger blur for a more prominent glass effect */
        border: 1px solid rgba(255, 255, 255, 0.2); /* More subtle border to enhance the glass look */
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Soft shadow */
    }

    .btn {
        font-size: 1rem;
        padding: 10px 20px;
        border-radius: 5px;
        font-weight: bold;
        background-color: transparent;
        border-color: #e443a6;
        color: #e443a6; /* Button text color */
        transition: color 0.3s ease;
    }

    .btn:hover {
        background-color: #e443a6;
        color: white; /* Change text color to white on hover */
    }

    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-transparent fixed-top border-bottom" style="border-color: #007bff; backdrop-filter: blur(10px);">
        <div class="container-fluid">
            <!-- Logo on the left -->
            <a class="navbar-brand" href="/home">
                <img src="path/to/logo.png" alt="Logo" style="width: 40px; height: 40px;">
            </a>
    
            <!-- Centered navigation buttons -->
            <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/home" style="color: #e443a6;">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/about" style="color: #e443a6;">About</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/services" style="color: #e443a6;">Services</a>
                    </li>
                </ul>
            </div>
    
            <!-- Admin button on the right -->
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/admin" style="color: #e443a6;">Admin</a>
                </li>
            </ul>
        </div>
    </nav>
    
    

    <!-- Fullscreen Banner Section -->
    <div class="banner">
        
    </div>

<!-- Tournament List Section -->
<div class="container mt-5">
    <!-- Info Turnamen Header -->
    <div class="text-center mb-5">
        <h2>Info Turnamen</h2>
        <div style="width: 150px; height: 3px; background-color: #e443a6; margin: 0 auto;"></div>
    </div>

    <div class="row g-4">
        <!-- Loop through tournaments and display each one in a card -->
        <% tournaments.forEach(tournament => { %>
            <div class="col-md-4">
                <!-- Wrap the card in an anchor tag to make it clickable -->
                <a href="/tournament/<%= tournament.id %>" class="text-decoration-none">
                    <div class="card glass-card shadow-lg border-0 rounded-3 overflow-hidden" style="transition: transform 0.3s ease;">
                        <div class="card-body">
                            <h5 class="card-title text-dark mb-3" style="font-size: 1.25rem; font-weight: bold;"><%= tournament.name %></h5>
                            <p class="card-text text-muted" style="font-size: 0.9rem;"><%= tournament.description %></p>
                            <p class="card-text text-dark" style="font-size: 0.9rem;">
                                <strong>Date:</strong> <%= new Date(tournament.date).toLocaleDateString('en-GB', { day: 'numeric', month: 'long', year: 'numeric' }) %>
                            </p>
                            <p class="card-text text-dark" style="font-size: 0.9rem;">
                                <strong>Location:</strong> <%= tournament.location %>
                            </p>
                            <!-- Button to view more details -->
                            <a href="/tournament/<%= tournament.id %>" class="btn btn-primary">View Details</a>
                        </div>
                    </div>
                </a>
            </div>
        <% }); %>
    </div>
</div>





</div><div style="height: 500px; background-color: #007bff; margin-top: 20px;">

</div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
